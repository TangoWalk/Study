#include<stdio.h>
#define LIST_INIT_SIZE 100
#define LISTINCREMENT 10

struct data_form{
    char elem;
    char prior;
    char next;
};

struct data_form stu_num,stu_name,stu_dpm,stu_pfss,stu_unit,stu_work;


void init_list(struct data_form L){//创建L表
    L.elem = (char *)calloc(LIST_INIT_SIZE,sizeof(char));
    if(!L.elem) exit("OVERFLOW");
    L.elem
}

void put_elem(struct data_form L,char e){//录入数据
    L.prior
}

void main()
{
    void init_list(struct data_form L);
    init_list(stu_num);
    /*
    init_list(stu_name)
    init_list(stu_dpm)
    init_list(stu_pfss)
    init_list(stu_unit)
    init_list(stu_work)
    */

	printf("OK");
}




	

void main()
{
	printf("OK");
}